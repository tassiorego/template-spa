import { Theme } from '../Theme';
import colors from './colors';
import {
  fontSizes, fonts, iconSizes, lineHeights,
} from './fonts';
import { radii, space, borderWidths } from './scales';

const theme: Theme = {
  colors,
  fontSizes,
  fonts,
  iconSizes,
  lineHeights,
  radii,
  space,
  borderWidths,
};

export default theme;
